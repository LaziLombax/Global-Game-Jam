using UnityEngine;

public class BubbleClusterManMa : MonoBehaviour
{
    public int totalBubblesInCluster = 1;
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void IncrementClusterAmount()
    {
        totalBubblesInCluster++;
    }
}
